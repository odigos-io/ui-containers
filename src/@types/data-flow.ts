export enum NODE_TYPES {
  HEADER = 'header',
  ADD = 'add',
  BASE = 'base',
  EDGED = 'edged',
  FRAME = 'frame',
  SCROLL = 'scroll',
  SKELETON = 'skeleton',
}

export enum EDGE_TYPES {
  LABELED = 'labeled',
}

export enum ADD_NODE_TYPES {
  ADD_RULE = 'addRule',
  ADD_SOURCE = 'addSource',
  ADD_ACTION = 'addAction',
  ADD_DESTINATION = 'addDestination',
}
